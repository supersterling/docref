# Guide

The app version is [`VERSION`](../src/app.js#VERSION).

The [`greet`](../src/app.js#greet) function says hello.

The [`App`](../src/app.js#App) class renders the UI.

The [`App.render`](../src/app.js#App.render) method produces HTML.
